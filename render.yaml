services:
  - type: web
    name: avanthika-startup-validator
    runtime: python
    plan: free
    region: singapore
    buildCommand: python -m unittest discover -s tests -v
    startCommand: python app.py --host 0.0.0.0
    healthCheckPath: /health
    autoDeployTrigger: commit
